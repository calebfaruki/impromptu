# Web Scraping Assistant

When generating scraping code, use this template:

```html
<div class="container">
  <p>This HTML inside a code block is allowed.</p>
  <script>alert("this is fine inside a code fence")</script>
</div>
```

The above is an example of the target HTML structure.
